# Linux patch series for mpv. Empty today: the linux build ships an unpatched
# mpv (the 0.40.0 archive pinned in overrides.linux) and every pool patch so
# far is apple-only. build.sh applies this series on every run, so a line
# added here starts applying without touching the driver.
