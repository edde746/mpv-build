# Windows patch series for mpv, applied after series.common (which carries
# the cross-platform fixes; the apple avfoundation/moltenvk stack lives in
# series.apple). Empty today: nothing is Windows-only yet. Entries are staged
# into the mpv-winbuild-cmake checkout and applied with `git apply` by the
# injected PATCH_COMMAND (see platforms/windows/pin_packages.py).
