# Android patch series for libplacebo, migrated from edde746/libmpv-android
# buildscripts/patches/libplacebo (its patch.sh applied the directory glob, so
# this order is that fork's lexicographic apply order; the upstream-commit-named
# file kept its name to stay byte-identical to the fork).
# One pool filename per line; order is authoritative and nothing is sorted.
0002-opengl-enable-ubos-on-gles.patch
c93aa134ab62365ce1177efff99b8e1e66a818e7.patch
