# Windows patch series for libass. Empty, and expected to stay small: the
# windows driver already applies series.common through pin_packages.py, which
# stages the resolved series (series.common then series.windows) into the
# winbuild checkout as packages/libass-NNNN-<pool>.patch. A windows-only patch
# would be one more staged file, not a change to that mechanism.
