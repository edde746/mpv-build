# Linux patch series for ffmpeg, applied after series.common to the 7.1
# archive pinned in overrides.linux. Empty today: the linux-only pool is
# empty and the videotoolbox/mediacodec stacks are apple/android-only.
# build.sh applies the resolved series on every run, so a line added here
# starts applying without touching the driver.
