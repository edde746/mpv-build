# Linux patch series for ffmpeg. Empty today: the linux build ships an
# unpatched ffmpeg (the 7.1 archive pinned in overrides.linux) and every pool
# patch so far is apple-only. build.sh applies this series on every run, so a
# line added here starts applying without touching the driver.
